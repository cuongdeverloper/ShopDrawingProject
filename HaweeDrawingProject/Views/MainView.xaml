<Window x:Class="HaweeDrawingProject.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MEP Pipe Exporter/Importer" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Chọn Level để thao tác:" FontWeight="Bold" Margin="0,0,0,5" />
        <ComboBox Grid.Row="1" ItemsSource="{Binding Levels}" SelectedItem="{Binding SelectedLevel}" 
                  DisplayMemberPath="Name" Height="25" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Export to JSON" Command="{Binding ExportCommand}" Width="120" Height="40" Margin="0,0,20,0"/>
            <Button Content="Import from JSON" Command="{Binding ImportCommand}" Width="120" Height="40"/>
        </StackPanel>
    </Grid>
</Window>